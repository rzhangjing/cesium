# ─── 前序代理杂散产物（防止误提交 / git add . 误入库）──────────────────────────
# 说明：cesiumrust/app_err.log 与 app_out.log 已被 git 跟踪，.gitignore 不会
# 自动 untrack 它们；此处仅防止同名/同类新产物被再次加入。若需彻底移出版本库，
# 需另行执行 `git rm --cached`（不在本次清理范围内）。
#
# 未使用裸 `*.log` 通配：仓库存在已跟踪的 .log 文件，按约定仅写作用域受限的模式。

# 代理运行日志（Robin 重跑 app / M0 修复轮可能再生成同类文件）
/app_err.log
/app_out.log
/build_*.log
/clippy_*.log
/demo_*.log
/diag*.log

# 代理测试/规格输出转储
/specs_output.txt
/test_output*.txt
/*_output.txt

# ─── pixel_diff 手工遗留测试图（F3：git add tools/ 会误入库）───────────────────
/tools/pixel_diff/test_*.png

# ─── M6 Wave A 集成（task #81）+ M11.3 headless 本机取证产物 ──────────────────
# `headless_smoke.png` 是 Robin #80（M11.3）的 smoke 产物；其余 PNG 是 task #81
# 的本机像素中性 / gate-ON 取证图（`v0_local` = 全 gate OFF 捕获，`v0_ctrl*` =
# 同二进制双跑对照组，`det_*` = STRICT_OFFLINE + FIXED_TIME 确定性对照，
# `m6_on` = M6 三 gate ON）。它们是**一次性本地证据**，不进版本库；权威取证基座
# 仍是已 `git add` 的 `specs/baselines/**` 与 `verification_evidence/**`。
# 同样不使用裸 `*.png` 通配（`specs/baselines`、`docs` 下有已跟踪 PNG）。
/headless_smoke.png
# FIX-GITIGNORE: M11.3 headless default capture name — `feature_flags::headless_output()`
# defaults to `headless_capture.png`, which the repo root carries as untracked (not
# ignored) evidence. Root-anchored, same style as `headless_smoke.png`; no bare `*.png`
# (would risk `specs/baselines` / `docs` tracked PNGs).
/headless_capture.png
/v0_local.png
/v0_off_post.png
/v0_ctrl*.png
/m6_on.png
/det_*.png
# `tools/m6wa_pixel_evidence.ps1`（task #81 取证 harness，为 M11.6 #64/#65/#66 预置）的三次捕获
/fin_off*.png
/fin_on.png

# 上述取证轮的 cargo / pixel_diff 输出转储（沿用「作用域受限模式，不用裸 *.log」约定）
/det_*.log
/fin_*.log
/m6_*_run.log
/m6_off_post.log
/ctrl*.log
/chk*.log
/chk_*.txt
# 本轮及后续代理在仓库根转储的 cargo test/clippy 日志（根锚定，不影响已跟踪的
# `verification_evidence/**/*.log`）。旧写法 `/test_m6wa*.log` 漏了 `test_fin.log`，
# 经 `git status --porcelain` 实测发现后改为 `/test_*.log`。
/test_*.log
# ─── 前序代理遗留的待删目录（防护，非处置）───────────────────────────────────
# `_obsolete_to_delete/` 是前序代理（M0/M2 视觉验证轮）暂存的 5 张已废弃取证 PNG
# （约 2.7 MB，`d2_terrain_dual*.png` / `d2_terrain_solo2.png` / `d3_dual.png` /
# `_diag_moved.png`），当前 untracked。目录名已声明意图，但**删除不可逆且不在
# task #81 授权范围**，故此处仅防止 `git add .` 把 2.7 MB 废弃图误入库；实际删除
# 交 leader 在最终清理轮裁定（同 `d:/Rust/cesium/_m11_3_spike/` 外层 spike crate）。
/_obsolete_to_delete/

# 注：`test_run.txt` 已被 git 跟踪（前序代理产物），写规则对它无效——同 `app_err.log`
# 情形，需 `git rm --cached` 才能移出版本库，不在本次清理范围。
# （阶段八：`app_err.log`/`app_out.log`/`test_run.txt` 已 `git rm`，不再跟踪。）

# ─── 阶段八补充：本仓代理转储的一次性日志（前一轮漏网，现物理删除 + 预防性忽略）───
# 下划线前缀的临时 check/smoke 日志、`d_cluster_*`/`e_*` 集群测跑转储。沿用
# 「作用域受限模式，不用裸 *.log」约定。
/_cctest.log
/_hl.log
/d_cluster_*.log
/e_clippy*.log
/e_cluster_test.log
/e_ff_test.log
