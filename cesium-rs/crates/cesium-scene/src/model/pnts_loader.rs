//! Ported from `packages/engine/Source/Scene/PntsLoader.js`.
//!
//! Loads point cloud tiles.

/// Loads point cloud (pnts) tiles.
pub struct PntsLoader;

impl PntsLoader {
    pub fn load(_data: &[u8]) -> Option<Vec<u8>> {
        None
    }
}
