//! `splitLongitude` – splits geometry at the international date line.

use crate::geometry::Geometry;

/// Splits a geometry that crosses the international date line.
///
/// TODO: full implementation.
pub fn split_longitude(geometry: &mut Geometry) {
    let _ = geometry;
}
