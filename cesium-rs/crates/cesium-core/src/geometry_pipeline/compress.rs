//! `compressVertices` – compresses normals and texture coordinates.

use crate::geometry::Geometry;

/// Compresses vertex attributes (normals, texture coordinates) for GPU storage.
///
/// TODO: full implementation — requires AttributeCompression integration.
pub fn compress_vertices(geometry: &mut Geometry) {
    let _ = geometry;
}
