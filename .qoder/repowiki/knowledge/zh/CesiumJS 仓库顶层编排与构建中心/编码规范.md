- 所有跨包公共脚本集中于 `scripts/`，由根 gulpfile 通过 Node require 调用，禁止在各子包内重复实现构建逻辑。
- ESLint/Prettier/Markdownlint/ast-grep 规则在根目录集中声明并通过 npm scripts 统一入口，子包不再单独维护风格配置。
- 构建产物一律输出到 `Build/` 目录，Source/Specs/Assets 保持源码态，发布前由 `makeZip` 任务打包。