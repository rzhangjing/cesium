- 每个指南子目录以 `README.md` 作为该主题的入口文件，并通过同级相对路径引用图片与附件。
- 截图资源统一放在 `Images/` 下，文件名采用 `<API>.<方法>.<场景>.png` 的三段式命名，便于在多个指南中复用。
- Fabric 材质示例与 Schema 一一对应：`Schemas/Fabric/Material.schema.json` 定义结构，`Schemas/Fabric/Examples/*.json` 提供可验证的最小示例。