npm run prepare 会执行 gulp prepare、安装 husky 钩子并在 CI 外自动安装 Playwright 浏览器依赖；npm run build/build-release/build-ts 走 Gulp 统一构建 JS 产物；npm run test/test-all/test-e2e 分别运行 Jasmine 单元测试与 Playwright e2e；cesium-rs 与 cesiumrust 各自通过 cargo 命令独立构建与测试，不在根 npm 脚本中直接调用。