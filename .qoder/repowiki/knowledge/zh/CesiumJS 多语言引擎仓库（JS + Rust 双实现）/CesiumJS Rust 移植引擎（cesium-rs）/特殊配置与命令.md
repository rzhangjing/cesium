GLSL shader 需通过 PowerShell 脚本 `crates/cesium-shaders/gen_all.ps1`、`gen_shader_modules.ps1`、`gen_structs.ps1` 以及 `crates/cesium-renderer/gen_skeletons.ps1` 重新生成 Rust 绑定；构建前必须执行这些脚本以保持生成的 `.rs` 与 `shaders/` 目录同步。