- JS 侧通过 Gulp 任务集中编排构建、测试、文档与发布流程，各子包不暴露独立 CLI。
- 测试数据集中在 Specs/Data 下，供 JS 与 Rust 两套实现共同引用以保证行为对齐。
- GLSL shader 与材质定义同时存在于 Documentation/Fabric 与 cesium-rs/shaders，作为 JS 与 Rust 渲染实现的共享契约。
- 代码风格由根级 .prettierrc、eslint.config.js、.editorconfig 统一约束到所有子模块。
- Git 钩子通过 .husky/pre-commit 在提交前触发 lint-staged 与 sg scan，跨 JS 与 Rust 变更统一生效。