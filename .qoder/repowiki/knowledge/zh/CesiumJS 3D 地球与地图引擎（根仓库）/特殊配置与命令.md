Node 要求 >=22；`npm run prepare` 会执行 `gulp prepare && husky && playwright install --with-deps` 安装浏览器依赖。常用命令：`npm run build`（构建全部）、`npm run test` / `--all` / `--webglValidation` / `--release`、`npm run test-e2e`（Playwright）、`cd cesiumrust && cargo test`（运行 Rust 端 Specs）。