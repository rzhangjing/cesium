根级常用命令：`npm run build`（依次构建 engine/widgets/Cesium）、`npm run test`（Karma 单元测试）、`npm run test-e2e`（Playwright e2e）、`npm run build-apps`（构建 Apps/Sandcastle）、`npm run start`（启动本地服务器）。Rust 侧在 `cesiumrust/` 目录下执行 `cargo build`/`cargo test`。