- JavaScript 源码按功能域拆分到 `packages/*/Source/**`，测试文件遵循 `<Name>Spec.js` 命名并由 Specs 下的集中配置加载
- 构建脚本集中在 `scripts/build.js`，通过 gulp 任务暴露统一的 build/watch/coverage/release 接口
- Rust 代码采用六边形架构，domain/crates 定义纯逻辑，adapters 实现 Bevy/网络/解码等外部适配
- 所有提交前通过 husky + lint-staged 执行 eslint/prettier/markdownlint 检查