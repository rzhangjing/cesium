本地调试：打开 `Specs/SpecRunner.html` 并在 URL 追加 `?category=...&spec=...&webglValidation=true&webglStub=true` 控制过滤与 WebGL 行为；Karma 执行：`karma start Specs/karma.conf.cjs`；E2E 截图更新：`npx playwright test --update-snapshots`（内部会调用 `npm run start -- --production` 启动服务）。