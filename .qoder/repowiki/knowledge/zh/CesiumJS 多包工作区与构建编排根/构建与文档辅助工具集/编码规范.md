- ast-grep 每个规则与其测试用例同名同 id，分别放在 rules/ 与 tests/ 下，并通过 __snapshots__/ 下的快照文件验证匹配结果。
- JSDoc 自定义标签统一通过 dictionary.defineTag(name, { onTagged(doclet, tag) {...} }) 模式注册，并在 cesiumTags.js 中集中导出。
- Rollup 插件遵循标准 { name, transform(code, id) } 接口，使用 createFilter 控制 include/exclude，并用 MagicString 原地改写源码而非返回新字符串。