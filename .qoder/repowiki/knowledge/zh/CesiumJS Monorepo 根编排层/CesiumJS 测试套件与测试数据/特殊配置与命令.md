浏览器单测需先构建产物到 `Build/CesiumUnminified/`，再执行 Karma；E2E 通过 `playwright.config.js` 自动拉起 `npm run start -- --production` 开发服务器，并在 Chromium 上强制 `--use-angle=gl` 启用 GPU WebGL 以提升渲染速度；`test.mjs` 可直接在 Node 下运行验证 npm 包导出。