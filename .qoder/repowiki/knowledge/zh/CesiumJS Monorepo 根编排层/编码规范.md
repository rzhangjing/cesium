- 所有子包通过 `@cesium/engine`、`@cesium/widgets` 等 npm workspace 别名相互引用，禁止跨包直接相对路径 import。
- 构建产物统一输出到 `Build/` 目录，开发模式使用 `Build/CesiumDev` 与 `Build/CesiumUnminified` 双通道，由 server.js 的 ContextCache 缓存 esbuild context。
- Source 文件变更通过 chokidar 监听并按 engine→widgets→Cesium 的顺序触发增量 rebuild，避免全量重打包。