esbuild（主打包器，target=es2020，支持 incremental context）、globby（源码/Spec 文件发现）、glsl-strip-comments（GLSL 压缩）、gulp v5（流式资源拷贝）、magic-string + rollup-pluginutils（遗留 strip-pragmas 插件）、JSDoc 3（自定义 publish.js + cesium_template 模板）。