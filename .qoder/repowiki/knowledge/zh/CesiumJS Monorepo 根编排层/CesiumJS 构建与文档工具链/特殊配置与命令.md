所有构建均通过根级 gulp 任务调用 scripts 中的函数，例如 `gulp build`、`gulp minify`、`gulp release`、`gulp generateDocumentation`、`gulp combine`；增量构建时 `bundleCesiumJs`/`bundleSpecs` 使用 `esbuild.context` 而非 `esbuild.build`，并在 `rebuild` 钩子里重写以处理内联 worker 写入。