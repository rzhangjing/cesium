构建需系统库：Ubuntu 上 `sudo apt install libudev-dev libasound2-dev libxkbcommon-dev vulkan-validationlayers`；运行完整测试套件用 `cargo test --workspace`，单独运行 specs 用 `cargo test -p specs`；发布构建启用 thin LTO 且 codegen-units=1 以优化二进制大小。