Karma 测试需先构建出 `Build/CesiumUnminified`，再通过 Karma 启动 Chrome（CI 使用 `--remote-debugging-port=9333` 的 `ChromeDebugging` 启动器）；E2E 测试由 Playwright 自动拉起 `npm run start -- --production` 服务并在 `http://localhost:8080` 访问，截图更新需加 `--update-snapshots` 参数。