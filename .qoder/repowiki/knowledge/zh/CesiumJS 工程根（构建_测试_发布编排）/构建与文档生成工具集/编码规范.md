- ast-grep 每条规则采用规则 YAML、同名测试 YAML 与对应 snapshot YAML 三件套组织，便于增量维护与回归验证。
- JSDoc 自定义标签统一通过 dictionary.defineTag 注册并在 onTagged 回调中向 doclet 注入字段或元数据。
- Rollup 插件遵循标准插件接口，在 transform 钩子中使用 createFilter 做文件级白黑名单过滤后再用 MagicString 原地改写源码。