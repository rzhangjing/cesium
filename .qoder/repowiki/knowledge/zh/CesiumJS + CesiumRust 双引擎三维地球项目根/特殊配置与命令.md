npm install（触发 prepare 钩子执行 gulp prepare && husky && playwright install --with-deps）后，可通过 npm run build 构建 JS、npm run test 运行 Karma Spec、npm run test-e2e 运行 Playwright E2E；Rust 侧在 cesiumrust/ 目录下用 cargo test/cargo run 独立编译与运行。