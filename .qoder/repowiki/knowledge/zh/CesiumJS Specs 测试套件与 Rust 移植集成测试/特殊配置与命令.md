JS 测试：`npm test`（Karma）、`npm run test-e2e`（Playwright e2e）；Rust 测试：`cargo test -p cesium-specs`。构建前需 `npm install` 触发 `prepare` 钩子安装 playwright 依赖。Specs 数据文件位于 `Specs/Data/`，Rust 测试数据随各 domain crate 内嵌。