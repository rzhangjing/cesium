首次安装需执行 `npm install`（触发 `prepare` 钩子自动运行 `gulp prepare`、husky 初始化，并在非 CI 环境安装 Playwright 浏览器依赖）；开发常用命令：`npm start` 启动本地服务器、`npm run build-watch` 增量构建、`npm test` 运行 Karma 单测、`npm run test-e2e` 运行 Playwright 端到端；Rust 子项目使用 `cargo build -p cesium-app` 或 `cargo run` 直接构建应用。